# OpenTrack-Easy 设计预案

本文档对应 Open Track 中当前最值得优先尝试、但暂不立即施工的两类增强：

1. 工具设计
2. Agent 架构设计

目标是保留一个比 `Menu.md` 更具体的技术预案，等主赛道稳定后再展开实现。

## 1. 设计定位

这一阶段的原则是：

- 不动课程统一检索器
- 不依赖外部搜索
- 不引入大规模训练
- 用尽量小的结构增强换取尽量稳定的收益

## 2. 工具设计

### 2.1 当前工具现状

当前仓库已有：

- `search(query)`
- `get_document(docid)`

对于复杂问题，这两者已经够用，但仍有明显短板：

- 全文过长，模型不一定能高效读完
- 找到文档后，缺少“在文档内定位证据”的手段

### 2.2 第一优先级工具：`find_in_document`

建议作为 Open Track 最简单工具增强项。

功能：

- 输入 `docid` 与 `keyword`
- 在指定文档内定位关键片段
- 返回局部上下文，而不是整篇全文

目标收益：

- 降低全文阅读成本
- 提高证据定位效率
- 减少模型在长文中丢关键信息的概率

建议接口：

```json
{
  "name": "find_in_document",
  "arguments": {
    "docid": "...",
    "keyword": "..."
  }
}
```

建议返回：

- `docid`
- `keyword`
- `matches`
- 每个 match 的简短上下文片段

### 2.3 第二优先级工具：文档片段读取

如果 `find_in_document` 效果不稳定，可考虑更简单的替代工具：

- `get_document_chunk(docid, start, end)`
- 或 `get_document_sections(docid)`

不过这类工具实现与使用成本更高，优先级低于 `find_in_document`。

### 2.4 不建议当前就做的工具

- 复杂重排序器
- 外部实体链接器
- 外部搜索接口
- 大量启发式工具链

原因：

- 违反或接近课程约束
- 实现复杂度大
- 贡献不容易与主赛道改进分离

## 3. 多 Agent 架构设计

### 3.1 设计原则

多 Agent 只有在它能明显改善“无效搜索”和“错误收尾”时才值得上。

否则：

- token 更多
- 轨迹更复杂
- 调试更难
- 评测波动更大

### 3.2 推荐的最小多 Agent 结构

如果后续进入实现，建议只尝试两角色结构。

方案 A：规划 agent + 执行 agent

- 规划 agent：决定下一步查什么
- 执行 agent：实际调用工具并整理结果

方案 B：执行 agent + 验证 agent

- 执行 agent：完成搜索与候选答案生成
- 验证 agent：检查证据是否足够、是否应继续搜索

在这两种里，更推荐方案 B。

原因：

- 当前主问题往往不是“不会搜”，而是“太早结束”
- 验证角色更容易直接减少错误收尾

### 3.3 推荐的最小协作流程

若后续实施验证型双 agent，可采用：

1. 主 agent 正常完成多轮搜索
2. 形成候选答案与证据摘要
3. 验证 agent 只回答一个问题：
“这些证据是否足以支持最终答案？如果不够，还缺什么？”
4. 若验证未通过，主 agent 再补一轮搜索

这样比“两个 agent 全流程并行搜索”更稳，也更容易归因。

### 3.4 当前阶段不建议做的多 Agent 方案

- 三角色以上复杂协作
- 并行检索多个子代理
- 带长期记忆的协作系统
- 大量 agent 间投票

原因：

- 对当前课程任务来说过重
- 很难在小样本评测上稳定体现收益

## 4. 评测策略

OpenTrack-Easy 阶段建议按单变量方式评测。

1. 先以 BaseDemand 版本作为对照组
2. 单独加入 `find_in_document`
3. 再单独加入验证 agent
4. 每一步都重新跑同一批样本

重点记录：

- 准确率变化
- 平均工具调用次数
- 平均展开文档数量
- 错误类型是否变化

## 5. 当前结论

Open Track 的最轻量且最合理顺序应为：

1. 先补一个文档内定位工具
2. 再尝试一个轻量验证 agent
3. 确认确有收益后，再考虑更复杂结构

在真正开始实施前，本文件无需继续展开到施工级别。
